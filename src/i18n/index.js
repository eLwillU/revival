import de from "./de";
import fr from "./fr";

// You can define more languages in the application by adding the like its been done here:
// After adding them you need to create a new json file which contains a key, which is paired with a value.
// for examples check the currently available language files.
export default {
  de: de,
  fr: fr,
};
